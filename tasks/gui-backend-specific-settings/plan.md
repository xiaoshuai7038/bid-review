Milestone 1: Settings Schema
- Acceptance:
  - 设置模型拆成 Claude / OpenCode 两套默认值。
  - 旧 `default_model` 仅作为兼容回退，不作为主展示字段。

Milestone 2: UI Split
- Acceptance:
  - 设置页有“通用配置”与“后端专属配置”切换。
  - 默认后端切换时，Claude / OpenCode 专属区域联动切换。

Milestone 3: Validation
- Acceptance:
  - 测试覆盖设置切换与模型拆分。
  - GUI screenshot 可见切换后的结构。

